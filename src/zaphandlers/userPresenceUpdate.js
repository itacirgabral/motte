/**
 * when a user's presence is updated
 * on (event: 'user-presence-update', listener: (update: PresenceUpdate) => void): this
 */
const userPresenceUpdate = () => {}

module.exports = userPresenceUpdate
