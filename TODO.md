From the types of disconnect reasons below, which one that I must destroy the existing session? In other words, the existing session instance cannot be used again?

badSession,
close,
intentional,
invalidsession,
Lost,
Replaced,
Timeout,
Unknown